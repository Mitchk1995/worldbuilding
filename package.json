{
  "name": "worldbuilding",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check:todo": "python -m tools.todo_check",
    "test:js": "node --test",
    "test:py": "python -m unittest discover -s test_python -p \"test_*.py\"",
    "test": "npm run check:todo && npm run test:js && npm run test:py"
  }
}
